<div class='w-75 p-3 mx-5 d-flex flex-column bd-highlight'>

  <h1>マイページ</h1>
  　<%# binding.pry %>
    <span class='h4'>お名前：<%= @user.name %>様</span>
    <div class='d-flex flex-column bd-highlight'>
      <% if @posts.present? %>
        <%= render @posts %>
      <% else %>
        <p><%= '.no_result' %></p>
      <% end %>
      </div>


</div>
